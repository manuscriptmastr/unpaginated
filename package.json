{
  "name": "fetch-all",
  "version": "1.0.0",
  "description": "Given a fetcher that expects a page or offset and a limit, orchestrate a master fetcher to retrieve all paginated entries as efficiently as possible.",
  "main": "fetch-all.js",
  "scripts": {
    "start": "node fetch-all.js",
    "test": "node fetch-all.test.js"
  },
  "author": "Joshua Martin",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.15",
    "node-fetch": "^2.6.0"
  }
}
